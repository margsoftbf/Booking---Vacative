import React from 'react'
import style from './Footer.module.css'

export default function Footer() {
  return (
    <div className={`${style.container}`}>
        <div className={`${style.footerLists}`}>
            <ul className={`${style.footerList}`}>
                <li className={`${style.footerItem}`}>Countries</li>
                <li className={`${style.footerItem}`}>Regions</li>
                <li className={`${style.footerItem}`}>Cities</li>
                <li className={`${style.footerItem}`}>Districts</li>
                <li className={`${style.footerItem}`}>Airports</li>
                <li className={`${style.footerItem}`}>Hotels</li>
            </ul>
            <ul className={`${style.footerList}`}>
                <li className={`${style.footerItem}`}>Countries</li>
                <li className={`${style.footerItem}`}>Regions</li>
                <li className={`${style.footerItem}`}>Cities</li>
                <li className={`${style.footerItem}`}>Districts</li>
                <li className={`${style.footerItem}`}>Airports</li>
                <li className={`${style.footerItem}`}>Hotels</li>
            </ul>
            <ul className={`${style.footerList}`}>
                <li className={`${style.footerItem}`}>Countries</li>
                <li className={`${style.footerItem}`}>Regions</li>
                <li className={`${style.footerItem}`}>Cities</li>
                <li className={`${style.footerItem}`}>Districts</li>
                <li className={`${style.footerItem}`}>Airports</li>
                <li className={`${style.footerItem}`}>Hotels</li>
            </ul>
            <ul className={`${style.footerList}`}>
                <li className={`${style.footerItem}`}>Countries</li>
                <li className={`${style.footerItem}`}>Regions</li>
                <li className={`${style.footerItem}`}>Cities</li>
                <li className={`${style.footerItem}`}>Districts</li>
                <li className={`${style.footerItem}`}>Airports</li>
                <li className={`${style.footerItem}`}>Hotels</li>
            </ul>
        </div>
    </div>
  )
}
